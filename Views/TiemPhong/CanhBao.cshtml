@model List<PetCareManagement.Models.TiemPhong>
@{ ViewData["Title"] = "Cảnh báo tiêm phòng"; }

<div class="card shadow-sm border-0">
    <div class="card-header bg-warning text-dark d-flex justify-content-between align-items-center">
        <h4 class="mb-0">⚠️ Cảnh báo tiêm phòng trong 7 ngày tới</h4>
        <a asp-action="Index" class="btn btn-outline-dark btn-sm">↩ Quay lại</a>
    </div>
    <div class="card-body">

        @if (!Model.Any())
        {
            <div class="alert alert-success mb-0">
                ✅ Không có lịch tiêm phòng nào trong 7 ngày tới.
            </div>
        }
        else
        {
            <p class="text-muted mb-3">Có <strong>@Model.Count</strong> lịch tiêm cần thực hiện trong 7 ngày tới.</p>

            <table class="table table-bordered align-middle">
                <thead class="table-light">
                    <tr class="text-center">
                        <th>Tên thú cưng</th>
                        <th>Chủ nuôi</th>
                        <th>Số điện thoại</th>
                        <th>Vaccine</th>
                        <th>Ngày tiêm tiếp</th>
                        <th>Còn lại</th>
                    </tr>
                </thead>
                <tbody>
                @{
                    var homNay = DateOnly.FromDateTime(DateTime.Today);
                }
                @foreach (var tp in Model)
                {
                    var conLai   = tp.NgayTiemTiep!.Value.DayNumber - homNay.DayNumber;
                    var rowClass = conLai <= 3 ? "table-danger" : "table-warning";
                    <tr class="@rowClass">
                        <td>@tp.MaTcNavigation?.TenThuCung</td>
                        <td>@tp.MaTcNavigation?.MaCnNavigation?.HoTen</td>
                        <td>@tp.MaTcNavigation?.MaCnNavigation?.SoDienThoai</td>
                        <td>@tp.TenVaccine</td>
                        <td class="text-center">@tp.NgayTiemTiep?.ToString("dd/MM/yyyy")</td>
                        <td class="text-center">
                            @if (conLai == 0)
                            {
                                <strong class="text-danger">Hôm nay!</strong>
                            }
                            else
                            {
                                <strong>Còn @conLai ngày</strong>
                            }
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        }

    </div>
</div>